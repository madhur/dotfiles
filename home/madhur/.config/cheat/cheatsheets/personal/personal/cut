Cut characters 1 through 10
    echo "This is a line of text" | cut -c 1-10
    This is a

Cut characters 1 through 10 of file
    cut ~/.bashrc

Cut from character 10 to end of each of line for file
    cut -c 10- ~/.bashrc

Give me the fifth field based on spaces
    echo "This is a line of text" | cut -d ' ' -f5
    of

Give me first field of /etc/passwd
    cut -d ":" -f1 /etc/passwd 

Gotcha with delimiter space (Use awk in this case)
    echo "abc    def" | cut -f2 -d ' '
    Empty result



