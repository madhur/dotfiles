Consume from topic
    kafkactl consume broadcasttopic --print-keys --print-timestamps  --from-beginning

Produce to topic
    kafkactl produce broadcasttopic --key=my-key --value="madhur ahuja7" 

Produce entire file
    kafkactl produce efGamePlay -f ~/github/perftestautomation/enrichment/payload/jsons/dealended.json --lineSeparator='###'

Create topic
    kafkactl create topic fantasymonitoringevents -V

Describe consumer group
    kafkactl describe consumer-group GROUP



