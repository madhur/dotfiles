# Firefly III daily financial digest config (loaded by firefly_digest.py).
# All keys are optional; the defaults below match the committed values.

MAIL_FROM=firefly-digest@madhur.co.in
MAIL_TO=ahuja.madhur@gmail.com

# Where FIREFLY_BASE_URL / FIREFLY_PAT live (same file the SMS/ICICI uploaders use).
FIREFLY_ENV_FILE=/home/madhur/Desktop/python/.env

# Reporting currency (drives the symbol; data is whatever Firefly returns).
CURRENCY=INR
